# commander.yml
laraboot-commander:
  commands:
    - name: Init
      run: |
        echo "Init"
    - name: NoOps
      run: |
        echo "Hello"
    - name: Install deps
      run: |
        echo "installing"